# Install Azure CLI
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
# Update Bicep
az bicep install && az bicep upgrade

az login --use-device-code
az account show
az account set --subscription MPN150
az group create --location switzerlandnorth --name 'rg-strg'
az deployment group what-if --template-file main.bicep --resource-group 'rg-strg'
az deployment group create --template-file main.bicep --resource-group 'rg-strg'