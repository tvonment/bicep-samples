# Install Azure CLI
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
# Update Bicep
az bicep install && az bicep upgrade

az login --use-device-code
az account show
az account set --subscription MPN150
az group create --location switzerlandnorth --name 'rg-vm'
az deployment group what-if --template-file main.bicep --resource-group 'rg-vm'
az deployment group create --template-file main.bicep --resource-group 'rg-vm'